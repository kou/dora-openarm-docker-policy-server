# An example Docker based policy server

## Build

```bash
docker build --tag openarm-docker-policy-server-example:1.0.0 .
```

## Run

TODO: Create test driver.

```bash
docker run -it --rm openarm-docker-policy-server-example:1.0.0
```
